<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.ACTION_MANAGE_OVERLAY_PERMISSION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPSSetter"
        tools:targetApi="34">
        <activity
            android:name=".ui.ActivitySettings"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:launchMode="singleTop">
        </activity>
        <activity
            android:name=".ui.MapActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- Launcher intent (tetap ada) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ============================================================
                 DEEP LINK: Intercept URL Google Maps
                 Mendukung: maps.google.com, www.google.com/maps,
                            goo.gl, maps.app.goo.gl
                 ============================================================ -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- maps.google.com -->
                <data android:scheme="http"  android:host="maps.google.com" />
                <data android:scheme="https" android:host="maps.google.com" />

                <!-- www.google.com/maps -->
                <data android:scheme="http"  android:host="www.google.com" android:pathPrefix="/maps" />
                <data android:scheme="https" android:host="www.google.com" android:pathPrefix="/maps" />

                <!-- google.com/maps -->
                <data android:scheme="http"  android:host="google.com" android:pathPrefix="/maps" />
                <data android:scheme="https" android:host="google.com" android:pathPrefix="/maps" />

                <!-- goo.gl short link -->
                <data android:scheme="http"  android:host="goo.gl" />
                <data android:scheme="https" android:host="goo.gl" />

                <!-- maps.app.goo.gl short link -->
                <data android:scheme="http"  android:host="maps.app.goo.gl" />
                <data android:scheme="https" android:host="maps.app.goo.gl" />
            </intent-filter>

            <!-- ============================================================
                 DEEP LINK: Intercept geo: URI scheme
                 Format: geo:-6.9667,110.4167 atau geo:0,0?q=-6.9,110.4
                 ============================================================ -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="geo" />
            </intent-filter>

        </activity>

        <service
            android:name=".utils.JoystickService"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDJHl2fdyya0PuEY-LRFUZM71pijfZN6IE" />
        <meta-data
            android:name="com.maplibre.AccessToken"
            android:value="pk.eyJ1IjoiZGVsdGFjaGF0IiwiYSI6ImNqc3c1aWczMzBjejY0M28wZmU0a3cwMzMifQ.ZPTH9dFJaav06RAu4rTYHw" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_desc" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/scopes" />
        <receiver
            android:name=".utils.StopGpsReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.jqssun.gpssetter.ACTION_STOP_GPS" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
